import ControllerHeader from './components/ControllerHeader'
import { connect } from 'react-redux'

const mapDispatchToProps = ( {

} )

export default connect(null, mapDispatchToProps)(ControllerHeader)
